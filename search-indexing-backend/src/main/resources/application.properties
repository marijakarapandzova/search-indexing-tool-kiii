spring.application.name=search-indexing-backend

springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

logging.level.root=INFO
logging.level.mk.ukim.finki.searchindexing=DEBUG
logging.level.org.springframework=WARN

logging.file.name=logs/app.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=30

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

jwt.secret=${JWT_SECRET_KEY}
jwt.expiration=864000000
jwt.header=Authorization
jwt.token-prefix=Bearer

vezilka.base-url=https://doniraj.vezilka.ai
vezilka.api-key=${VEZILKA_API_KEY:}

# Crawl-index pipeline (see CrawlerProperties)
crawler.max-pages-per-job=100
crawler.same-domain-only=true
crawler.request-delay-ms=1000
crawler.user-agent=EMC-SearchIndexingBot/1.0 (+https://finki.ukim.mk)

# Local search engine (see SearchProperties) — used by the SearchIndex you implement
search.index-path=./data/search-index
search.uri=
search.index-name=documents

spring.profiles.active=dev
